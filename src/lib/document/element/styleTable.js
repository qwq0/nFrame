/**
 * 样式单位对照表
 */
export const styleNumTable = {
    /** 仅数字 */
    number: CSS.number,
    /** 百分数 */
    percent: CSS.percent,
    
    // length
    /** (长度)相对字体 */
    em: CSS.em,
    /** (长度)相对x-height */
    ex: CSS.ex,
    /** (长度)相对0的单位 */
    ch: CSS.ch,
    ic: CSS.ic,
    /** (长度)相对根元素的字体大小 */
    rem: CSS.rem,
    lh: CSS.lh,
    rlh: CSS.rlh,
    /** (长度)相对视口宽(百分数) */
    vw: CSS.vw,
    /** (长度)相对视口高(百分数) */
    vh: CSS.vh,
    vi: CSS.vi,
    vb: CSS.vb,
    /** (长度)相对视口短边(百分数) */
    vmin: CSS.vmin,
    /** (长度)相对视口长边(百分数) */
    vmax: CSS.vmax,
    /** (长度)厘米 */
    cm: CSS.cm,
    /** (长度)毫米 */
    mm: CSS.mm,
    Q: CSS.Q,
    in: CSS.in,
    /** (长度)点 */
    pt: CSS.pt,
    /** (长度)派卡 */
    pc: CSS.pc,
    /** (长度)像素 */
    px: CSS.px,
    
    // angle
    deg: CSS.deg,
    grad: CSS.grad,
    rad: CSS.rad,
    turn: CSS.turn,

    // time
    /** (时间)秒 */
    s: CSS.s,
    /** (时间)毫秒 */
    ms: CSS.ms,

    // frequency
    Hz: CSS.Hz,
    kHz: CSS.kHz,

    // resolution
    dpi: CSS.dpi,
    dpcm: CSS.dpcm,
    dppx: CSS.dppx,

    // flex
    fr: CSS.fr
};

export const styleNameTable = {

};
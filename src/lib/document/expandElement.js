import { Nelement } from "./element/Nelement.js";

/**
 * 展开元素
 * 将元素对象
 * @param {import("./interface/EleData").EleData} obj
 * @returns {Nelement}
 */
export function expandElement(obj)
{
    var now = new Nelement();
    if(obj)
}